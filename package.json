{
  "name": "jobmatchai",
  "version": "1.0.5",
  "private": true,
  "description": "JobMatch AI Chrome extension — devDependencies are tests only; the extension itself ships as plain JS files with no build step.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "hooks": "git config core.hooksPath .githooks && echo '✓ pre-push hook installed (warns if sibling jobmatchai-h1b-data is >90 days stale). Override with: git push --no-verify'"
  },
  "devDependencies": {
    "happy-dom": "^15.0.0",
    "vitest": "^2.0.0"
  }
}
